# Public model asset

Place the browser-safe model here as:

`house-existing.glb.dat`

The file contents should be valid GLB bytes; only the filename extension is changed. Keep any private source scans, renovation notes, measurements, addresses, or metadata out of this public repository.
